services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - ./server/.env
    volumes:
      - ./data/db:/app/server/data
      - ./data/uploads:/app/server/uploads
    restart: unless-stopped
